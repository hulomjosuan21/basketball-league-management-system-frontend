import 'package:flutter/material.dart';

class AdministratorPlayerScreen extends StatelessWidget {
  const AdministratorPlayerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
